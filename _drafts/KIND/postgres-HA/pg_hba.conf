# Local connections
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Allow replication connections
host replication replicator all md5

# Allow connections for all other users
host all all all md5